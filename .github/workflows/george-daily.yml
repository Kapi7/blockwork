name: George Daily

on:
  schedule:
    - cron: '17 10 * * *'
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Call George
        env:
          SYNC_TOKEN: ${{ secrets.SYNC_TOKEN }}
        run: |
          curl -sS -X POST "https://blockwork-91h.pages.dev/api/george/post?token=${SYNC_TOKEN}" | head -c 3000
          echo ""
